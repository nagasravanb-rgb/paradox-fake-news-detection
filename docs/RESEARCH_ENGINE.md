# Research engine

QUESTION → decompose (heuristic sentences / optional LLM JSON) → plan → search per subquestion → filter duplicates → extract snippets → contradiction pass → synthesis with explicit conflicts and uncertainties.

Reports never hide disagreement. Empty retrieval → `NO_EVIDENCE`.
