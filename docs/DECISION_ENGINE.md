# Decision engine

Scores options with transparent weights (evidence_support, risk, constraint_fit, uncertainty_penalty).

If research/situation retrieved no evidence, recommendation is `null` (`NO_RECOMMENDATION`) rather than a fake “do X”.
